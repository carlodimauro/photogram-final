<h1><%= @the_user.username %></h1>

<dl>
  <dt>Private</dt>
  <dd><%=@the_user.private%></dd>

  <dt>Followers</dt>
  <dd>
    <%= @the_user.followers.count %>

    <div>
      <!--<a href="/delete_follow_request/1968">Unfollow</a>-->
    </div>

  </dd>

  <dt>Following</dt>
  <dd> <%= @the_user.following.count %></dd>
</dl>

<hr>

<ul>
  <li>
    <a href="/users/<%= @the_user.username %>">Profile</a>
  </li>
  <li>
    <a href="/users/<%= @the_user.username %>/liked_photos">Liked photos</a>
  </li>
  <li>
    <a href="/users/<%= @the_user.username %>/feed">Feed</a>
  </li>
  <li>
    <a href="/users/<%= @the_user.username %>/discover">Discover</a>
  </li>
</ul>

<hr>



<h2>Liked photos (<%= @the_user.liked_photos.count %>)</h2>

<table border="1">
  <tr>
    <th>
      Image
    </th>

    <th>
      Owner
    </th>

    <th>
      Caption
    </th>

    <th>
      Posted
    </th>

    <th>
      Likes
    </th>

    <th></th>
  </tr>

<% @the_user.liked_photos.order(created_at: :desc).each do |liked_p| %>
  


  <tr>
    <td>
      
       <% if liked_p.read_attribute(:image).start_with?('http') %>
          <img src="<%= liked_p.read_attribute(:image) %>" class="img-responsive">
        <% else %>
          <img src="<%=liked_p.image %>" class="img-responsive">
        <% end %>
    </td>

    <td>
      <%= liked_p.owner_id %>
    </td>

    <td>
      <%= liked_p.caption %>
    </td>

    <td>
      <%= time_ago_in_words(liked_p.created_at) %>
    </td>

    <td>
      <%= liked_p.likes_count %>
    </td>

    <td>
      <a href="/photos/<%=liked_p.id%>">
        Show details
      </a>
    </td>
  </tr>
<% end %>
</table>
