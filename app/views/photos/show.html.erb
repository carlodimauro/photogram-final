<h1>
  Photo Details
</h1>

<dl>

  <dt>
    Image
  </dt>
  <dd>

<% if @the_photo.read_attribute(:image).start_with?('http') %>
  <img src="<%= @the_photo.read_attribute(:image) %>" class="img-responsive">
<% else %>
  <img src="<%=@the_photo.image %>" class="img-responsive">
<% end %>

  </dd>

  <dt>
    Caption
  </dt>
  <dd>
    <%= @the_photo.caption %>
  </dd>

  <dt>
    Owner
  </dt>
  <dd>
    <%= @the_photo.owner.username %>
  </dd>

  <dt>
    Posted
  </dt>
  <dd>
    <%= time_ago_in_words(@the_photo.created_at) %> ago
  </dd>

  <dt>
    Likes count
  </dt>
  <dd>
    <%= @the_photo.likes_count %>
  </dd>

  <dt>
    Comments count
  </dt>
  <dd>
    <%= @the_photo.comments_count %>
  </dd>

  <dt>
    Fans
  </dt>
  <dd>
    <% if @the_photo.fans.empty? %>
      <!-- Handle the case when there are no fans -->
    <% else %>
      <%= @the_photo.fans.map(&:username)[0..-2].join(', ') %><%= @the_photo.fans.size > 1 ? ', and ' : '' %><%= @the_photo.fans.last.username %>
    <% end %>
  </dd>

  <dt>
    Add a fan
  </dt>
  <dd>

    <% likedalready = Like.where(fan_id: current_user.id, photo_id: @the_photo.id).first %>
    <% if likedalready.present?%>
      <a href="/delete_like/<%=likedalready.id%>">Unlike</a>
    <%else%>
      <form action="/insert_like" method="post">
        <input type="hidden" name="query_photo_id" value=<%= @the_photo.id%>>
        <input type="hidden" name="query_user_id" value=<%= current_user.id%>>
        <button>Like</button>
      </form>

    <%end%>
  </dd>

  <% if current_user.id == @the_photo.owner_id%>
    <dt>
      Edit photo
    </dt>
    <dd>
      <form action="/modify_photo/<%=@the_photo.id%>" method="post">
        <label for="browser_image">Image URL</label>
        <input id="browser_image" type="text" name="query_image" placeholder="Enter a URL for the image..." value="<%= @the_photo.image %>">

        <label for="browser_caption">Caption</label>
        <textarea id="browser_caption" name="query_caption" placeholder="Enter a caption for the photo..."><%= @the_photo.caption %></textarea>

        <input type="hidden" name="query_owner_id" value="<%= current_user.id %>">
        <input type="hidden" name="query_location" value="<%= @the_photo.location %>">
        <input type="hidden" name="query_likes_count" value="<%= @the_photo.likes_count %>">
        <input type="hidden" name="query_comments_count" value="<%= @the_photo.comments_count %>">
        <button>Update photo</button>

      </form>
    </dd>

    <dt>
      Delete photo
    </dt>
    <dd>
      <a href="/delete_photo/<%= @the_photo.id %>">Delete this photo</a>
    </dd>
  <%end%>
</dl>

<hr>

<h2>Comments</h2>

<table>
  <tr>
    <th>Commenter</th>
    <th>Comment</th>
    <th>Posted</th>
  </tr>

  <% @the_photo.comments.each do |a_comment| %>

    <tr>
      <td>
        <% un = User.where( :id => a_comment.author_id).first %>
        <%= un.username%>
      </td>

      <td>
        <%= a_comment.body%>
      </td>

      <td>
        <%= time_ago_in_words(a_comment.created_at)%>
      </td>
    </tr>

  <% end %>
</table>

<hr>

<h3>Add a new comment</h3>

<form action="/insert_comment" method="post">
  <input type="hidden" value="<%= @the_photo.id%>" name="query_photo_id">

  <label for="browser_comment">Comment</label>
  <textarea id="browser_comment" name="query_body" placeholder="Enter a comment..."></textarea>

  <input type="hidden" name="query_commenter_id" value="<%= current_user.id %>">

  <button>Add comment</button>
</form>

<!--<div>
  <div>
    <h2>
      Edit photo
    </h2>

    <form action="/modify_photo/<%#= @the_photo.id %>"  method="post" >
      <div>
        <label for="caption_box">
          Caption
        </label>

        <input type="text" id="caption_box" name="query_caption" value="<%#= @the_photo.caption %>">
      </div>

      <div>
        <label for="image_box">
          Image
        </label>

        <input type="text" id="image_box" name="query_image" value="<%#= @the_photo.image %>">
      </div>

      <div>
        <label for="owner_id_box">
          Owner
        </label>

        <input type="text" id="owner_id_box" name="query_owner_id" value="<%#= @the_photo.owner_id %>">
      </div>

      <div>
        <label for="location_box">
          Location
        </label>

        <input type="text" id="location_box" name="query_location" value="<%#= @the_photo.location %>">
      </div>

      <div>
        <label for="likes_count_box">
          Likes count
        </label>

        <input type="text" id="likes_count_box" name="query_likes_count" value="<%#= @the_photo.likes_count %>">
      </div>

      <div>
        <label for="comments_count_box">
          Comments count
        </label>

        <input type="text" id="comments_count_box" name="query_comments_count" value="<%#= @the_photo.comments_count %>">
      </div>

      <button>
        Update photo
      </button>
    </form>
  </div>
</div>

<hr>
-->
